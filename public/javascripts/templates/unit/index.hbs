<h1>unit index page</h1>
	{{#if isEditing}}
		{{partial "unit/form"}}
		<button {{action 'done'}}>Done</button>
	{{else}}
		<button {{action 'edit'}}>Edit</button>
	{{/if}}

	
{{partial "unit/record"}}
{{!-- {{view App.UnitView unitBinding=unit}} --}}