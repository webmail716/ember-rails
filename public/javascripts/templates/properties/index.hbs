<h1>Properties index page</h1>

<ul>
	{{#each property in controller}}
		{{!-- {{render "unit/_record"  contentBinding=unit controller='App.unitIndexController'}} --}}
		{{!-- <li>Unit number: {{unit.unit_number}}</li> --}}

		{{partial "properties/record" propertyBinding=property}} 
		{{!-- unitBinding=unit --}}
		{{!-- {{unit-details}} --}}
		
		{{#linkTo 'property.index' property}}Show{{/linkTo}}

		{{!-- {{render "unit/_record" unit controller='App.unitIndexController'}} --}}
		{{!-- {{render "unit/_actions" unit}} --}}
		{{!-- {{view App.UnitIndexView templateName="unit/_record" controller=App.UnitIndexController}} --}}
	{{!-- {{view App.UnitDetailsView unitBinding=unit}} --}}
	{{/each}}
	<li><br/></li>
</ul>

{{#linkTo 'properties.new'}}New property{{/linkTo}}

<br/>

{{outlet}}

